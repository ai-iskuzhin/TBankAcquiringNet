namespace TBankAcquiringNet;

/// <summary>
/// Признак регистрации автоплатежа (Recurrent) в методе Init. Передается только при регистрации.
/// </summary>
public enum TBankRecurrent
{
    /// <summary>Зарегистрировать рекуррентный (родительский) платеж (Y).</summary>
    Yes
}
